const initialState = {
    userDetail:{
    }
};
export default (state = initialState, action = {}) => {
    let changes = {};

    switch (action.type) {
        default:
            return state;
    }

};